# Wallet private key (required for real trading)
# Export from Magic.link (Polymarket settings > Export Private Key)
PRIVATE_KEY=0x1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef

# Your Polymarket wallet address (the proxy wallet, NOT the Magic.link EOA)
# Find this on Polymarket website - it's your deposit address
FUNDER_ADDRESS=0x31321312

# Signature type - IMPORTANT: Must match your wallet type!
# 0 = EOA (MetaMask connected directly, not through Polymarket)
# 1 = Poly Proxy (Magic.link / email sign-up) - DEFAULT for most users
# 2 = Gnosis Safe
SIGNATURE_TYPE=1

# Polymarket API credentials (OPTIONAL - auto-generated from wallet if not provided)
# These are derived from your wallet, not from a Polymarket dashboard
# Builder API keys can be used here if you have them
# POLY_API_KEY=
# POLY_API_SECRET=
# POLY_API_PASSPHRASE=

# Strategy settings (prices in dollars, e.g., 0.95 = $0.95)
ENTRY_THRESHOLD=0.95    # Buy when price >= this (min entry)
MAX_ENTRY_PRICE=0.98    # Don't buy above this (avoid ceiling)
STOP_LOSS=0.80          # Sell when price drops to this level
STOP_LOSS_DELAY_MS=5000 # Wait 5 seconds before confirming stop-loss (filters false triggers)
MAX_SPREAD=0.03         # Skip markets with bid-ask spread > $0.03
TIME_WINDOW_MINS=5      # Only enter within 5 minutes of expiry

# Risk mode: "normal" or "super-risk"
# normal: Entry $0.95-0.98, Stop $0.80, 5min window
# super-risk: Entry $0.70-0.95, Stop $0.40, no time limit (HIGH RISK!)
RISK_MODE=normal

# Bot settings
POLL_INTERVAL_MS=10000  # Backup polling interval (real-time via WebSocket handles entries/stop-losses)

# Paper trading (simulation mode)
PAPER_TRADING=false     # Set to true to simulate trades with virtual money
PAPER_BALANCE=100       # Starting balance for paper trading

# Compound limit (profit taking)
COMPOUND_LIMIT=15       # Take profit when balance exceeds this (0 = disabled)
BASE_BALANCE=10         # Reset to this balance after taking profit
